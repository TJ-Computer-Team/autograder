#include <bits/stdc++.h>
using namespace std;
#define int long long
using vi = vector<int>;
using pi = pair<int, int>;
#define p pair
#define pb push_back
#define f first
#define s second
int dx[4] = {1, -1, 0, 0};
int dy[4] = {0, 0, -1, 1};
signed main(){
	ios::sync_with_stdio(false);
        cin.tie(0);
        //freopen("test.in", "r", stdin);
        int t = 1;
        //cin>>t;
        while(t--){
		cout<<"HI"<<endl;
        }
}
